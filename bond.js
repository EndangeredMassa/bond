// Generated by CoffeeScript 1.3.3
(function() {
  var arrayEqual, bond, createReturnSpy, createThroughSpy, enhanceSpy,
    __slice = [].slice;

  createThroughSpy = function(getValue) {
    var spy;
    spy = function() {
      var args, isConstructor, result;
      args = Array.prototype.slice.call(arguments);
      spy.calledArgs[spy.called] = args;
      spy.called++;
      isConstructor = Object.keys(this).length === 0;
      result = getValue.apply(this, args);
      if (isConstructor) {
        return this;
      }
      return result;
    };
    return enhanceSpy(spy, getValue);
  };

  createReturnSpy = function(getValue) {
    var spy;
    spy = function() {
      var args;
      args = Array.prototype.slice.call(arguments);
      spy.calledArgs[spy.called] = args;
      spy.called++;
      return getValue.apply(this, args);
    };
    return enhanceSpy(spy, getValue);
  };

  enhanceSpy = function(spy, original) {
    spy.prototype = original.prototype;
    spy.called = 0;
    spy.calledArgs = [];
    spy.calledWith = function() {
      var args, lastArgs;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      if (!spy.called) {
        return false;
      }
      lastArgs = spy.calledArgs[spy.called - 1];
      return arrayEqual(args, lastArgs);
    };
    return spy;
  };

  arrayEqual = function(A, B) {
    var a, b, i, _i, _len;
    for (i = _i = 0, _len = A.length; _i < _len; i = ++_i) {
      a = A[i];
      b = B[i];
      if (a !== b) {
        return false;
      }
    }
    return true;
  };

  bond = function(obj, property) {
    var previous, returnMethod, through, to;
    previous = obj[property];
    if (!(previous != null)) {
      throw new Error("Could not find property " + property + ".");
    }
    to = function(newValue) {
      var unregistered;
      unregistered = false;
      afterEach(function() {
        if (unregistered) {
          return;
        }
        obj[property] = previous;
        return unregistered = true;
      });
      obj[property] = newValue;
      return obj[property];
    };
    returnMethod = function(returnValue) {
      var unregistered;
      unregistered = false;
      afterEach(function() {
        if (unregistered) {
          return;
        }
        obj[property] = previous;
        return unregistered = true;
      });
      obj[property] = createReturnSpy(function() {
        return returnValue;
      });
      return obj[property];
    };
    through = function() {
      var unregistered;
      unregistered = false;
      afterEach(function() {
        if (unregistered) {
          return;
        }
        obj[property] = previous;
        return unregistered = true;
      });
      obj[property] = createThroughSpy(previous);
      return obj[property];
    };
    return {
      to: to,
      "return": returnMethod,
      through: through
    };
  };

  if (typeof window !== "undefined" && window !== null) {
    window.bond = bond;
  }

  if (typeof module !== "undefined" && module !== null) {
    module.exports = bond;
  }

}).call(this);
